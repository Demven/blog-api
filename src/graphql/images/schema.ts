export const imagesDefinitions = `
  type Image {
    _id: String
    url: String
    description: String
    credits: String
  }
`;

export const imagesQuery = ``;

export const imagesMutations = ``;
